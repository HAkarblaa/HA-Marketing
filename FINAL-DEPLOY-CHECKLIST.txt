HA Marketing - Final corrected package

Code-side final QA and cleanup completed:
1) shop link on homepage now opens the real shop
2) shop/cart/orders/admin remain connected to the real Supabase shop tables
3) transport customer orders now use the logged-in Supabase profile instead of fake customer data
4) driver page now requires a real approved employee account and real vehicle details entered by that driver
5) fake driver names/phones/cars and simulated driver ETA were removed
6) taxi/tuktuk rating button now stores the real rating in Firebase
7) ride chat/call paths were included in the final Firebase rules file
8) domino and penalty online-room paths were included in the final Firebase rules file
9) transport notification listener now watches the actual "searching" ride status
10) fake games wallet/profile/stats dashboard was removed; games page only links to games that actually exist
11) building materials now opens the real shop category instead of a placeholder catalog
12) football card now opens the real football-news page
13) internal file references and inline JavaScript were statically checked

IMPORTANT CLOUD STATE:
- Supabase Edge Functions were already deployed in the live project during setup.
- Firebase Rules in the live console must match firebase-database.rules.json in this package.
  The previous live rules only allowed rides/dominoRooms, while the final code also uses rideChats, rideCalls and penaltyRooms.
- Electronic payments and automatic SMS/WhatsApp still require real third-party provider accounts/credentials; the site does not fake these services.
